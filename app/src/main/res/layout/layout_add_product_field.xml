<?xml version="1.0" encoding="utf-8"?>
<!-- Một field: label nhỏ + EditText pill. Dùng include + override id ở nơi gọi. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/space_md"
    android:orientation="vertical">

    <TextView
        android:id="@+id/fieldLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_body"
        android:textStyle="bold"
        tools:text="Tên sản phẩm" />

    <EditText
        android:id="@+id/fieldInput"
        android:layout_width="match_parent"
        android:layout_height="@dimen/admin_field_height"
        android:layout_marginTop="@dimen/space_sm"
        android:background="@drawable/bg_admin_field"
        android:paddingHorizontal="@dimen/space_lg"
        android:textColor="@color/text_primary"
        android:textColorHint="@color/text_muted"
        android:textCursorDrawable="@null"
        android:textSize="@dimen/text_body"
        tools:hint="Pre-order" />
</LinearLayout>
