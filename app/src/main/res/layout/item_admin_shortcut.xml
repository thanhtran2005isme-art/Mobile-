<?xml version="1.0" encoding="utf-8"?>
<!-- Card lối tắt: icon cam + tên + mô tả -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_admin_card"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:minHeight="@dimen/admin_shortcut_min_height"
    android:orientation="vertical"
    android:padding="@dimen/space_lg">

    <ImageView
        android:id="@+id/shortcutIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        tools:src="@drawable/ic_admin_grid" />

    <TextView
        android:id="@+id/shortcutTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_md"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_subtitle"
        android:textStyle="bold"
        tools:text="Sản phẩm" />

    <TextView
        android:id="@+id/shortcutDesc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xs"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="@color/text_muted"
        android:textSize="@dimen/text_small"
        tools:text="Danh sách, thêm, sửa xóa sản phẩm" />

</LinearLayout>
