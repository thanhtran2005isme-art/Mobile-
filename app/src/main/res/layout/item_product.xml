<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingBottom="@dimen/space_md">

    <!-- Cover + heart + shop tag -->
    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="@dimen/product_image_height"
        app:cardCornerRadius="@dimen/radius_lg"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/productImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:scaleType="centerCrop"
                tools:src="@drawable/bg_product_1" />

            <!-- Heart góc trên-phải -->
            <FrameLayout
                android:id="@+id/productLikeWrap"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="end|top"
                android:layout_marginTop="@dimen/space_sm"
                android:layout_marginEnd="@dimen/space_sm"
                android:background="@drawable/bg_heart_circle">

                <ImageView
                    android:id="@+id/productLike"
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_heart_outline" />
            </FrameLayout>

            <!-- Shop tag góc dưới-trái -->
            <TextView
                android:id="@+id/productShop"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start|bottom"
                android:layout_marginStart="@dimen/space_sm"
                android:layout_marginBottom="@dimen/space_sm"
                android:background="@drawable/bg_shop_tag"
                android:paddingHorizontal="@dimen/space_sm"
                android:paddingVertical="3dp"
                android:textColor="@color/text_link"
                android:textSize="@dimen/text_caption"
                android:textStyle="bold"
                tools:text="ksksn" />

        </FrameLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Giá -->
    <TextView
        android:id="@+id/productPrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_subtitle"
        android:textStyle="bold"
        tools:text="100.000.000đ" />

    <!-- Pre-order tag + tên -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xs"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/productPreorder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_preorder_tag"
            android:paddingHorizontal="@dimen/space_sm"
            android:paddingVertical="2dp"
            android:text="@string/tag_preorder"
            android:textColor="@color/text_link"
            android:textSize="@dimen/text_caption"
            android:visibility="gone"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/productName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/space_sm"
            android:layout_weight="1"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/text_secondary"
            android:textSize="@dimen/text_body"
            tools:text="áo sơ mi" />

    </LinearLayout>

</LinearLayout>
